# Adobe PDF Services API — OAuth Server-to-Server credentials.
# Get them at https://developer.adobe.com/console (Add API → PDF Services API → OAuth Server-to-Server).
PDF_SERVICES_CLIENT_ID=your_client_id_here
PDF_SERVICES_CLIENT_SECRET=your_client_secret_here
